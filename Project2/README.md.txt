To compile and run follow the usual xv6 compilation. You will require gcc-multilib(for 32 bit support) and qemu.


RUN:

make

make qemu


TEST:

The test program is called getcount.c which can be run within xv6 shell by doing ./getcount


